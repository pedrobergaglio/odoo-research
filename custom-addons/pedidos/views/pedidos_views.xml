<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Tipos de Orden -->
    <record id="view_sale_order_type_form" model="ir.ui.view">
        <field name="name">sale.order.type.form</field>
        <field name="model">sale.order.type</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="code"/>
                        <field name="active"/>
                    </group>
                    <group>
                        <field name="description"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="view_sale_order_type_tree" model="ir.ui.view">
        <field name="name">sale.order.type.tree</field>
        <field name="model">sale.order.type</field>
        <field name="type">list</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
                <field name="code"/>
                <field name="active"/>
            </list>
        </field>
    </record>

    <record id="action_sale_order_type" model="ir.actions.act_window">
        <field name="name">Tipos de Orden</field>
        <field name="res_model">sale.order.type</field>
        <field name="view_mode">list,form</field>
    </record>

    <!-- Extensión de la vista de órdenes de venta -->
    <record id="view_sale_order_form_inherit" model="ir.ui.view">
        <field name="name">sale.order.form.inherit</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="arch" type="xml">
            <field name="partner_id" position="after">
                <field name="order_type_id" options="{'no_create': True, 'no_open': True}"/>
                <field name="is_pedido" invisible="1"/>
            </field>
        </field>
    </record>

    <!-- Vista de pedidos (órdenes filtradas por tipo Pedido) -->
    <record id="action_pedidos" model="ir.actions.act_window">
        <field name="name">Pedidos</field>
        <field name="res_model">sale.order</field>
        <field name="view_mode">list,form,calendar,pivot,graph,activity</field>
        <field name="domain">[('is_pedido', '=', True)]</field>
        <field name="context" eval="{'default_order_type_id': ref('pedidos.sale_order_type_pedido')}"/>        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Crea tu primer pedido
            </p>
        </field>
    </record>

    <!-- Filtros predefinidos para las vistas -->
    <record id="view_sale_order_search_inherit" model="ir.ui.view">
        <field name="name">sale.order.search.inherit</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_sales_order_filter"/>
        <field name="arch" type="xml">
            <filter name="my_sale_orders_filter" position="after">
                <filter string="Pedidos" name="filter_pedidos" domain="[('is_pedido', '=', True)]"/>
            </filter>
        </field>
    </record>
</odoo>